name: "Setup pnpm environment"
description: "Setup pnpm, Node.js, and install dependencies"

inputs:
  node-version:
    description: "Node.js version"
    default: "22.x"
  registry-url:
    description: "NPM registry URL for publishing"
    default: ""
  install-playwright:
    description: "Whether to install Playwright browsers"
    default: "false"
  skip-install:
    description: "Skip installing dependencies (e.g. when restoring from cache)"
    default: "false"

runs:
  using: "composite"
  steps:
    - name: Setup pnpm
      uses: pnpm/action-setup@v6
      with:
        run_install: false

    - name: Setup Node.js ${{ inputs.node-version }}
      uses: actions/setup-node@v6
      with:
        node-version: ${{ inputs.node-version }}
        registry-url: ${{ inputs.registry-url }}
        cache: "pnpm"
        cache-dependency-path: pnpm-lock.yaml

    - name: Install dependencies
      if: inputs.skip-install != 'true'
      run: pnpm install --frozen-lockfile
      shell: bash

    - name: Install Playwright Browsers
      if: inputs.skip-install != 'true' && inputs.install-playwright == 'true'
      run: pnpm exec playwright install --with-deps chromium
      shell: bash
